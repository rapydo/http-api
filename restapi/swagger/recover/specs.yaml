
depends_on:
  - MAIN_LOGIN_ENABLE

file: endpoints
class: RecoverPassword 
baseuri: /auth

mapping:
  ask_recover: "/recover"
  do_recover: "/recover/<token_id>"

labels:
  - base
  - authentication
