common:
  custom:
    # publish: true
    authentication: true

    # BEWARE: NOW IS VERIFIED INSIDE THE CODE
    # authorized:
    #   - admin_root


  custom_parameters:
    - AdminUsers

users:
  summary: Create a new user
  # description: -
  responses:
    '200':
      description: The uuid of the new user is returned